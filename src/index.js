import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';


ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

// to run frontend \spot: npm start
  // frontend runs at http://localhost:3000/
// to run backend \spot\server: npm run start
  // backends runs at http://localhost:5000/
